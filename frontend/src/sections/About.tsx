import { Typography } from "@mui/material";

const About = () => (
  <div>
    <Typography variant="h6">About Us</Typography>
    <Typography>
      This dashboard allows users to manage pet profiles, contact details, and
      view adoption resources.
    </Typography>
  </div>
);

export default About;
